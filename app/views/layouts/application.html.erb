<!DOCTYPE html>
<html data-theme="emerald">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-8EZZPJTNSY"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-8EZZPJTNSY');
    </script>

    <script src="https://code.jquery.com/jquery-latest.js"></script>
    <script type="text/javascript">
      $(function() {
          var height=$("#header").height();
          $("body").css("margin-top", height + 10);//10pxだけ余裕をもたせる
      });
    </script>

    <style>
      html {
        scroll-padding-top: 5rem;
      }
    </style>

    <%= display_meta_tags(default_meta_tags) %>
    <%= favicon_link_tag('favicon.ico') %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script async
      src="https://maps.googleapis.com/maps/api/js?key=<%= Rails.application.credentials.google_map_api_key %>&libraries=places">
    </script>

    <%= stylesheet_link_tag 'application', "data-turbo-track": 'reload' %>
    <%= javascript_include_tag 'application', "data-turbo-track": 'reload', defer: true %>
  </head>

  <body>
    <div class="flex flex-col min-h-screen">
      <%= render 'shared/header' %>
      <div class="grow">
        <%= render 'shared/flash/flash_messages' %>
        <%= yield %>
      </div>
      <%= render 'shared/footer' %>
    </div>
  </body>
</html>
