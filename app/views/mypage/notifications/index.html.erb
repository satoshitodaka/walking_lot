<% set_meta_tags title: '通知一覧' %>
<div class="container mx-auto px-4 py-10 max-w-lg">
  <p class="text-2xl font-bold">通知一覧</p>
  <% if @user_notifications.present? %>
    <!--Notification Card-->
    <%= render partial: 'notification', collection: @user_notifications, as: :user_notification %>
  <% else %>
    <p class="text-base font-bold my-5">お知らせはありません</p>
  <% end %>
</div>
